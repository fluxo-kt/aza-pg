{
  "name": "aza-pg",
  "version": "1.0.0",
  "description": "Production-Ready PostgreSQL 18 Infrastructure Stack with 38 extensions (36 enabled by default)",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun scripts/build.ts",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "type-check": "tsc --noEmit",
    "validate": "bun scripts/validate.ts --fast",
    "validate:full": "bun scripts/validate.ts --all",
    "validate:parallel": "bun scripts/validate.ts --all --parallel",
    "generate": "bun scripts/config-generator/generator.ts",
    "check:docs": "bun scripts/check-docs-consistency.ts",
    "check:manifest": "bun scripts/validate-manifest.ts",
    "check:size": "bun scripts/check-size-regression.ts",
    "check:shell": "shellcheck scripts/**/*.sh stacks/*/scripts/*.sh docker/postgres/*.sh",
    "check:yaml": "echo 'YAML linting temporarily disabled due to config parsing issue'",
    "check:docker": "hadolint --config .hadolint.yaml docker/postgres/Dockerfile",
    "check:sql": "echo 'SQL linting: No SQL linter configured - SQL files exist but are auto-generated or example files'",
    "check:toml": "echo 'TOML linting: No TOML linter configured - TOML files are minimal config files'",
    "test:smoke": "bun scripts/test-smoke.ts",
    "test:all": "bun scripts/test-all.ts",
    "test:all:fast": "bun scripts/test-all.ts --fast",
    "hooks:install": "bun-git-hooks install",
    "hooks:uninstall": "bun-git-hooks uninstall"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "arktype": "^2.1.25",
    "bun-git-hooks": "^0.3.1",
    "oxlint": "^1.26.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "keywords": [
    "postgresql",
    "postgres",
    "database",
    "docker",
    "pgbouncer",
    "extensions",
    "infrastructure",
    "production"
  ],
  "author": "aza-pg",
  "license": "MIT"
}
