{
  "timescaledb": {
    "min": 0.2,
    "max": 0.35,
    "description": "TimescaleDB time-series extension (current optimized build)"
  },
  "pg_stat_monitor": {
    "min": 0.05,
    "max": 0.12,
    "description": "Enhanced query performance monitoring (optimized build)"
  },
  "vector": {
    "min": 0.1,
    "max": 0.3,
    "description": "pgvector - vector similarity search (optimized build)"
  },
  "pgroonga": {
    "min": 0.25,
    "max": 0.7,
    "description": "PGroonga full-text search (optimized build)"
  },
  "timescaledb_toolkit": {
    "min": 12.0,
    "max": 12.8,
    "description": "TimescaleDB analytical hyperfunctions (versioned shared library)"
  },
  "pg_jsonschema": {
    "min": 3.0,
    "max": 3.4,
    "description": "JSON Schema validation (Rust/pgrx, current optimized build)"
  },
  "wrappers": {
    "min": 0.25,
    "max": 0.45,
    "description": "Supabase foreign data wrappers (Rust/pgrx, versioned shared library)"
  },
  "vectorscale": {
    "min": 0.8,
    "max": 1.1,
    "description": "pgvectorscale DiskANN index (versioned prebuilt GitHub release binary)"
  }
}
